repos:
  # ─────────────────────────────────────────────────────────────────────
  # 🧰 Built-in hooks for config, formatting, etc.
  # ─────────────────────────────────────────────────────────────────────
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: check-case-conflict
      - id: check-merge-conflict
      - id: check-toml
      - id: check-yaml
      - id: check-json
      - id: pretty-format-json
        args: [--autofix, --no-sort-keys]
      - id: end-of-file-fixer
      - id: trailing-whitespace
        exclude: \.py$  # Ruff handles Python files

  # ─────────────────────────────────────────────────────────────────────
  # ✨ Ruff for lint + format
  # ─────────────────────────────────────────────────────────────────────
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.11.6
    hooks:
      - id: ruff
        args: [--exit-non-zero-on-fix]
      - id: ruff-format

  # ─────────────────────────────────────────────────────────────────────
  # 🧪 Typing, formatting, and security tools via official mirrors
  # ─────────────────────────────────────────────────────────────────────
  - repo: https://github.com/psf/black
    rev: 23.1.0
    hooks:
      - id: black
        args: [
          --line-length=100,
          --target-version=py39
        ]

  - repo: https://github.com/PyCQA/bandit
    rev: 1.7.4
    hooks:
      - id: bandit

  - repo: https://github.com/codespell-project/codespell
    rev: v2.2.4
    hooks:
      - id: codespell

  - repo: https://github.com/igorshubovych/markdownlint-cli
    rev: v0.44.0
    hooks:
      - id: markdownlint
        name: markdownlint
        entry: markdownlint
        language: node
        types: [markdown]
        args: [--fix]

  # ─────────────────────────────────────────────────────────────────────
  # 🧱 Local system hooks (must be installed via pyproject + uv)
  # ─────────────────────────────────────────────────────────────────────
  - repo: local
    hooks:
{% if cookiecutter.cli_tool == "y" %}
      - id: cog
        name: cog
        entry: cog
        language: system
        types: [markdown]
        require_serial: true
        args: [-r, "README.md"]
{% endif %}

{%- if cookiecutter.deptry == 'y' %}
      - id: deptry
        name: deptry
        entry: uv run deptry .
        language: system
        types: [python]
{%- endif %}

      - id: vulture
        name: vulture
        entry: vulture
        language: system
        types: [python]
        args: [
          "{{cookiecutter.project_slug}}",
          "tests",
          "--min-confidence", "80"
        ]

      - id: safety
        name: safety
        entry: safety
        language: system
        types: [python]
        args: [
          "check", "--full-report"
        ]

      - repo: local
        hooks:
            - id: mypy
              name: mypy
              entry: uv run mypy
              language: system
              types: [python]
